<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mezzomedia.core.repository.mybatis.MybatisUserMapper">

	<select id="userList" >
		SELECT
		 user_id AS id,
		 user_name AS userName
		FROM user
	</select>
</mapper>